appId: com.trueperformancenetwork.gratis
productName: Gratis
directories:
  output: release/

# node-llama-cpp needs filesystem access for native .node binaries and mmap'd model files
asar: true
asarUnpack:
  - "node_modules/node-llama-cpp/**"

publish:
  provider: github
  owner: taofu-labs
  repo: gratis-ai

# --- macOS ---
mac:
  category: public.app-category.productivity
  artifactName: "Gratis-mac-${arch}.${ext}"
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  notarize: true
  target:
    - target: dmg
      arch:
        - arm64
        - x64
    - target: zip
      arch:
        - arm64
        - x64

# --- Windows ---
win:
  artifactName: "Gratis-win.${ext}"
  target:
    - target: nsis
      arch:
        - x64

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true

# --- Linux ---
linux:
  category: Utility
  artifactName: "Gratis-linux.${ext}"
  target:
    - target: AppImage
      arch:
        - x64
